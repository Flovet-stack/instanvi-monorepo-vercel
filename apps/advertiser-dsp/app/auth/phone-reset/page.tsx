'use client';

// eslint-disable-next-line @nx/enforce-module-boundaries
import { PhoneResetPage } from '@instanvi-monorepo/auth';

/* eslint-disable-next-line */
export interface NumberResetProps {}

const NumberReset: React.FC<NumberResetProps> = () => {
  return <PhoneResetPage />;
};

export default NumberReset;
