{
  "name": "publisher-ssp",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/publisher-ssp",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/publisher-ssp"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/publisher-ssp"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "publisher-ssp:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "publisher-ssp:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "publisher-ssp:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "publisher-ssp:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/publisher-ssp/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/publisher-ssp/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
