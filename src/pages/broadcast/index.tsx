import { HeaderLayout } from '@/layouts/header-layout';
import BroadcastFeature from '@/features/broadcast';
import React from 'react';

const Broadcast = () => {
  return (
    <HeaderLayout>
      <BroadcastFeature />
    </HeaderLayout>
  );
};

export default Broadcast;
